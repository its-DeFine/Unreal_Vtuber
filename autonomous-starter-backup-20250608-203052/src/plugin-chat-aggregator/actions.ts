import { Action } from '@elizaos/core';

/**
 * Chat Aggregator Actions
 * Basic actions for chat aggregation functionality
 */

export const chatActions: Action[] = [
  // Add chat-specific actions here when needed
]; 