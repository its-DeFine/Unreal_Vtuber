import { Provider } from '@elizaos/core';

/**
 * Chat Aggregator Providers
 * Basic providers for chat aggregation functionality
 */

export const chatProviders: Provider[] = [
  // Add chat-specific providers here when needed
]; 