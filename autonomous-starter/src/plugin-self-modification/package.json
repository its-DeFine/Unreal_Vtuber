{
  "name": "@elizaos/plugin-self-modification",
  "version": "0.1.0",
  "description": "Plugin enabling agents to modify their own character through reflection and self-learning",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@elizaos/core": "workspace:*",
    "fast-xml-parser": "^4.3.2",
    "jsonpath-plus": "^7.2.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
