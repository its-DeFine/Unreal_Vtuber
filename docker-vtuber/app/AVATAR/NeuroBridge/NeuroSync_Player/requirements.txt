# pygame==2.6.1 # Moved to Dockerfile
keyboard==0.13.5
timecode==1.4.1
pandas
requests==2.32.3
# PyAudio==0.2.14 # Moved to Dockerfile
soundfile==0.13.1
pydub==0.25.1
scipy
openai==1.75.0
redis==4.6.0
dotenv
aiohttp==3.9.5
# AutoGen for V3 orchestrator
ag2>=0.2.0
# NumPy compatibility fix for librosa/numba (pin to 1.x to avoid ABI issues)
numpy>=1.24.0,<2.0.0
# Audio processing dependencies with compatible versions
librosa>=0.10.0,<0.11.0
numba>=0.57.0,<0.60.0

# Reactive VTuber System dependencies
watchdog>=3.0.0  # For hot-reloading character files
pyyaml>=6.0  # For YAML character configuration
dataclasses-json>=0.6.0  # For better dataclass serialization
python-dateutil>=2.8.0  # For date/time handling