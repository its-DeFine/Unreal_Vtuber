name: Orchestrator Health Tests

on:
  push:
    branches:
      - codex/wake-sleep-logic
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install pytest fastapi starlette
      - name: Run tests
        run: |
          PYTHONPATH="$GITHUB_WORKSPACE" pytest orchestrator-health/tests
