/**
 * Standalone execution is intentionally disabled.
 * This package must be booted by the NanoClaw plugin runtime.
 */

console.error(
  "[negotiator] Standalone startup is disabled. Load this package via NanoClaw plugin `agent-negotiator`."
);
process.exit(1);
